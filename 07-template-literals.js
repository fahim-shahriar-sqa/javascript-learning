const environment = "QA";
const baseUrl = "https://api.company.com";
const userId = 101;

console.log(`Running API tests in ${environment} environment.`);

console.log(`Fetching user from ${baseUrl}/users/${userId}.`);